import { firebaseConfig } from '../app/auth/firebase.config';

export const environment = {
  production: true,
  imgSrc: 'https://storage.googleapis.com/morningharwood-server.appspot.com/',
  firebaseConfig
};
